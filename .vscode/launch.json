{
    "version": "0.2.0",
    "configurations": [
        {
        "name": "Debug with LLDB",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/out/debug/program.exe",
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "make fresh",
        }
    ]
} 